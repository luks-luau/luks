[package]
name = "websocket"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mlua-sys = { version = "0.10", features = ["luau"], default-features = false }
tungstenite = { version = "0.24", features = ["native-tls"] }
native-tls = "0.2"
url = "2"
