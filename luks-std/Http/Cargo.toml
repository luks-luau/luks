[package]
name = "http"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mlua-sys = { version = "0.10", features = ["luau"], default-features = false }
ureq = { version = "2", features = ["json", "tls"] }
serde_json = "1"
urlencoding = "2"
